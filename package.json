{
  "name": "chatstyle-ai",
  "version": "1.0.0",
  "description": "An elegant tool that beautifies AI conversation outputs",
  "scripts": {
    "start": "serve src",
    "build": "mkdir -p dist && cp -r src/* dist/",
    "dev": "serve src"
  },
  "dependencies": {
    "serve": "^14.0.0"
  }
} 